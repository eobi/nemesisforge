# Nemesis Forge — pinned runtime for the Linux Docker image + the Windows package.
# Core (always): NOTHING. The engine core is standard library only.
# Every line below is an optional lens. Without them the engine still runs and
# says in its output which lens was unavailable.
# Engine extras (Docker/full): symbolic lens + closed-binary coverage.
angr
frida
# Dev / self-verify:
pytest>=8
httpx>=0.27
